import Layout from "../../components/Layout"

const add_user = () => {
    return (
        <Layout
            content={(
            <div>
                Add User
            </div>
    
            )}
        />
    )
}

export default add_user
