import Layout from "../../components/Layout"

const user_list = () => {
    return (
        <Layout
            content={(
            <div>
                User List
            </div>
    
            )}
        />
    )
}

export default user_list
