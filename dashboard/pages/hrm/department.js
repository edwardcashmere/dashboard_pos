import Layout from "../../components/Layout"

const department = () => {
    return (
        <Layout
            content={(
            <div>
                Department
            </div>
    
            )}
        />
    )
}

export default department
