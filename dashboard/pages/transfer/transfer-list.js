import Layout from "../../components/Layout"

const transfer_list = () => {
    return (
        <Layout
            content={(
            <div>
                Transfer List
            </div>
    
            )}
        />
    )
}

export default transfer_list
