import Layout from "../../components/Layout"

const add_transfer = () => {
    return (
        <Layout
            content={(
            <div>
                Add Transfer
            </div>
    
            )}
        />
    )
}

export default add_transfer
