import Layout from "../../components/Layout"
const purchase_list = () => {
    return (
        <Layout
            content={(
            <div>
                Purchase List
            </div>
    
            )}
        />
    )
}

export default purchase_list
