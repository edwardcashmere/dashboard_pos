import Layout from "../../components/Layout"

const account_list = () => {
    return (
        <Layout
            content={(
            <div>
                Account List 
            </div>
    
            )}
        />
    )
}

export default account_list
