import Layout from "../../components/Layout"

const add_account = () => {
    return (
        <Layout
            content={(
            <div>
                Add Account
            </div>
    
            )}
        />
    )
}

export default add_account
