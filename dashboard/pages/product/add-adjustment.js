import Layout from "../../components/Layout"
const add_adjustment = () => {
    return (
        <Layout
        content={(
            <div>
                Add Adjustment
            </div>
        )}
        />
    )
}

export default add_adjustment
