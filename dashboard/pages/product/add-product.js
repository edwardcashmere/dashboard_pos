import Layout from "../../components/Layout"

const add_product = () => {
    return (
        <Layout
            content={(
            <div>
                Add Product
            </div>
    
            )}
        />
    )
}

export default add_product;
