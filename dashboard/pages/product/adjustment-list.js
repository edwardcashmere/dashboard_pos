import Layout from "../../components/Layout"

const adjustment_list = () => {
    return (
        <Layout
        content={(
        <div>
            Adjustment List
        </div>
        )}
        />
    )
}

export default adjustment_list;
