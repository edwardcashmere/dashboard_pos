import Layout from "../../components/Layout"

const product_list = () => {
    return (

        <Layout
        content={(
        <div>
            Product List
        </div>

        )}
        />
    )
}

export default product_list
