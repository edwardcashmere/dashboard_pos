import Layout from "../../components/Layout"

const print_barcode = () => {
    return (
        <Layout
        content={(
        <div>
            Print Barcode
        </div>

        )}
        />
    )
}

export default print_barcode;
