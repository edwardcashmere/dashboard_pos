import Layout from "../../components/Layout"

const add_expense = () => {
    return (
        <Layout
            content={(
            <div>
                Add Expense
            </div>
    
            )}
        />
    )
}

export default add_expense
