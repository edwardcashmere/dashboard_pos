import Layout from "../../components/Layout"

const add_quotation = () => {
    return (
        <Layout
            content={(
            <div>
                Add quotation
            </div>
    
            )}
        />
    )
}

export default add_quotation
