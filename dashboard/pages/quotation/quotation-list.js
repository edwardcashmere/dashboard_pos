import Layout from "../../components/Layout"

const quotation_list = () => {
    return (
        <Layout
            content={(
            <div>
                Quotation List
            </div>
    
            )}
        />
    )
}

export default quotation_list
