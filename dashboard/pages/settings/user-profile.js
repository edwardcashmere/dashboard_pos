import Layout from "../../components/Layout"

const user_profile = () => {
    return (
        <Layout
            content={(
            <div>
                User Profile
            </div>
    
            )}
        />
    )
}

export default user_profile
